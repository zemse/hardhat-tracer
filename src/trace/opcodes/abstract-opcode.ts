import { InterpreterStep } from "@nomicfoundation/ethereumjs-evm";
import { Item } from "../transaction";

// export interface Opcode<AbstractParams extends Item["params"]> {
//   name: string;
//   parseStep: (step: InterpreterStep) => AbstractParams;
// }
